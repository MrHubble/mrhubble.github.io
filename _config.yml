collections:
  rails:
    output: true
  react_native:
    output: true

defaults:
  - scope:
      path: ""
      type: "rails"
    values:
      layout: "default"
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

# Build settings
theme: minima

sass:
  sass_dir: _sass
  style: compressed